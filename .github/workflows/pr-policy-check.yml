name: PR Policy Check

on:
  pull_request:
    paths:
      - 'terraform/**'
      - 'clusters/**'
      - 'policies/**'

jobs:
  policy-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      id-token: write   # for OIDC AWS auth

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_CI_ROLE_ARN }}
          aws-region: eu-west-1

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.10"

      - name: Install checkov and OPA
        run: |
          pip install checkov
          curl -sL https://openpolicyagent.org/downloads/latest/opa_linux_amd64 -o /usr/local/bin/opa
          chmod +x /usr/local/bin/opa

      - name: Terraform init (all changed module directories)
        run: |
          git diff --name-only origin/main...HEAD \
            | grep -E '\.tf$' \
            | sed 's|/[^/]*\.tf$||' \
            | sort -u \
            | while read -r dir; do
                echo "Init: $dir"
                terraform -chdir="$dir" init -backend=false -input=false
              done

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Run policy check loop
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash loops/pr-policy-check.sh
